buildscript {
    repositories {
        google()  // Make sure Google repository is included
        mavenCentral()  // If needed for other dependencies
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.3.1'  // Add the correct version
    }
}

allprojects {
    repositories {
        google()  // Ensure this is also included in the allprojects block
        mavenCentral()
    }
}
